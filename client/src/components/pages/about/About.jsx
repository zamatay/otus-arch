import React, {useEffect, useRef, useState} from 'react';

const About = ({ token, userId }) => {

    return (
        <div>
            <h1>Добро пожаловать!</h1>
            <p>Это главная страница</p>
        </div>
    );
};

export default About;
